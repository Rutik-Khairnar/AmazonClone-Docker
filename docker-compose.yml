# version: "3.8"

# services:
#   web:
#     image: node:18-alpine
#     container_name: amazon-clone
#     working_dir: /app
#     volumes:
#       - ./:/app
#       - /app/node_modules
#     ports:
#       - "3000:3000"
#     command: sh -c "
#       npm install --legacy-peer-deps --no-package-lock &&
#       echo '📦 Checking for outdated packages...' &&
#       npm outdated || true &&
#       echo '🚀 Starting dev server...' &&
#       npm run dev -- --host 0.0.0.0 --port 3000"


version: "3.8"

services:
  amazonclone:
    image: node:18
    container_name: amazonclone
    working_dir: /app
    volumes:
      - ./:/app
    command: sh -c "
      npm install --legacy-peer-deps --no-package-lock &&
      echo '📦 Checking for outdated packages...' &&
      npm outdated || true &&
      echo '🚀 Starting dev server...' &&
      npm run dev -- --host 0.0.0.0 --port 3000"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.amazonclone.rule=Host(`amazon.local`)"
      - "traefik.http.services.amazonclone.loadbalancer.server.port=3000"
    networks:
      - web

  traefik:
    image: traefik:v2.10
    container_name: traefik
    command:
      - "--api.insecure=true"
      - "--providers.docker=true"
      - "--entrypoints.web.address=:80"
    ports:
      - "80:80"       # App accessible via amazon.local
      - "8081:8080"   # Traefik dashboard
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock:ro"
    networks:
      - web

networks:
  web:






